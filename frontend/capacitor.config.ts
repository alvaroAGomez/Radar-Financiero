import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.radarfinanciero.app',
  appName: 'Radar Financiero',
  webDir: 'dist/radar-financiero-app/browser'
};

export default config;
